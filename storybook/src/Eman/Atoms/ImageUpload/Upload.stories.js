import React from "react";
import { Upload } from "./Upload";

export default {
  title: "Eman/Atoms/ImageUpload",
  component: Upload,
};

const Template = (args) => <Upload {...args} />;
export const upload = Template.bind({});
